مستندات فنی و مرجع جامع پلتفرم «سحاب» (پارت اول)
هدف سند: این سند راهنما به هر توسعه‌دهنده (انسان یا هوش مصنوعی) اجازه می‌دهد بدون داشتن دسترسی مستقیم به محیط محلی (Laragon) یا پایگاه داده اصلی سحاب، ساختار داده‌ها، روابط کلیدها و منطق پلتفرم را کاملاً درک کرده و اقدام به توسعه قالب (Child Theme)، افزونه‌های جانبی یا هماهنگ‌سازی موتور sahab-sync نماید.

۱. معماری کلان و زیرساخت (System Architecture)
پلتفرم سحاب بر پایه سیستم مدیریت محتوای وردپرس پیاده‌سازی شده است. اصل اساسی در طراحی این پلتفرم، جداسازی مطلق منطق سیستم (Logic) از لایه نمایش (Presentation) است.

محیط توسعه محلی: Laragon

لایه منطق و ذخیره‌سازی داده: افزونه‌ی اختصاصی همگام‌سازی (sahab-sync) و افزونه‌ی مدیریت فیلدهای پیشرفته (ACF Free).

لایه نمایش: پوسته فرزند (Child Theme) جهت تضمین عدم پاشیدگی کدها هنگام به‌روزرسانی قالب اصلی.

مکانیزم همگام‌سازی: غیرمتمرکز، مبتنی بر شناسه‌های یکتای جهانی (UUID) و سیستم اعتبارسنجی هش محتوا (Content Hashing).

۲. ساختار هسته دیتابیس و نگاشت پست‌ها (Core Database & Post Mapping)
محتوای اصلی پلتفرم سحاب (اخبار، گزارش‌ها و واکاوی‌های منطقه‌ای) در جداول استاندارد وردپرس ذخیره می‌شود، اما به واسطه فیلدهای سفارشی، رفتاری کاملاً تحلیلی و دیتابیس‌محور به خود گرفته است.

جدول مبدا پست‌ها: wp_posts
فقط پست‌هایی با مشخصات زیر در چرخه پردازش و سینک سحاب قرار می‌گیرند:

post_type = post (یا هر پست تایپ اختصاصی که تعیین شود)

post_status = publish

جدول متاداده پست‌ها: wp_postmeta
تمام داده‌های ارزش‌افزوده و فیلدهای شاخص تحلیلی پلتفرم سحاب در این جدول به صورت کلید/مقدار (meta_key / meta_value) ذخیره می‌شوند.

۳. کاتالوگ دقیق فیلدهای سفارشی پست‌ها (Post Custom Fields Catalog)
هر توسعه‌دهنده یا هوش مصنوعی برای تولید خروجی یا نوشتن کوئری روی پست‌ها، باید از این کلیدهای یکتا استفاده کند:

الف) فیلدهای شاخص و تحلیلی (اطلاعات تصاویر ACF)
نام فنی فیلد (meta_key)	نوع فیلد در ACF	نوع داده در دیتابیس	توضیحات و مقادیر استاندارد
automation_id	عدد (Number)	longtext / int	شماره اتوماسیون اداری مربوط به پرونده.
subject	چک‌باکس (Checkbox)	longtext (آرایه سریالایز شده)	
موضوعات تحلیلی کلان. مقادیر مجاز خروجی:


- political-clics: روحانیون سیاسی


- prominent-clerics: روحانیون شاخص


- institutions: موسسات


- international: بین الملل


- dogmatism: تحجر


- preacher: وعاظ و مبلغین

event_date	متن (Text)	longtext	تاریخ وقوع رویداد (ترجیحاً به صورت متن شمسی یا ساختار توافق‌شده).
classification	انتخاب (Select)	longtext	
سطح طبقه‌بندی سند. مقادیر مجاز:


- normal: عادی


- confidential: محرمانه


- highly_confidential: خیلی محرمانه


- secret: سری


- top_secret: به کلی سری

priority	انتخاب (Select)	longtext	
درجه ارجحیت و فوریت. مقادیر مجاز:


- normal: عادی


- urgent: فوری


- immediate: آنی

news_type	انتخاب (Select)	longtext	
نوع و لایه خبر ارسالی. مقادیر مجاز:


- open: آشکار


- official: رسمی


- technical: فنی


- cyber: سایبری


- hidden: منبع


- ravi: راوی

evaluation	انتخاب (Select)	longtext	
ارزیابی صحت‌سنجی خبر. مقادیر مجاز:


- valid: صحت دارد


- probably_valid: احتمالاً صحت دارد


- under_review: در دست بررسی


- invalid: صحت ندارد

ب) فیلدهای سیستم پیوست چندگانه (Sequential Attachments)
به دلیل استفاده از نسخه رایگان ACF و محدودیت فیلد گالری، پیوست‌ها به صورت یک آرایه متوالی ساختاریافته ذخیره می‌شوند. سیستم حداکثر ۳ فایل را پشتیبانی می‌کند:

کلیدهای فنی متاداده (meta_key):

attachment_file_1

attachment_file_2

attachment_file_3

نوع داده ذخیره شده: شناسه عددی یکتای رسانه در وردپرس (Attachment ID / Integer).

منطق بازیابی در سیستم خروجی: توسعه‌دهنده یا هوش مصنوعی نباید خودِ عدد را به عنوان فایل فرض کند؛ بلکه باید با خواندن این ID، تابع تبدیل آن به آدرس مستقیم (URL) را صدا بزند:

PHP
// نمونه منطق بازیابی برای هر فیلد پیوست
$file_id = get_post_meta($post_id, 'attachment_file_1', true);
if ($file_id) {
    $file_url = wp_get_attachment_url($file_id); // لینک مستقیم فایل جهت قرارگیری در پکیج سینک
}
این پایان پارت اول مستندات است. در پاسخ بعدی (پارت دوم)، لایه کامنت‌های تحلیلی و فیلد سازمانی مسئولین اداره را کاملاً فرموله و مستند می‌کنیم. تایید بفرمایید تا پارت دوم را ارسال کنم.

مستندات فنی و مرجع جامع پلتفرم «سحاب» (پارت دوم)
این بخش از سند، به تشریح لایه‌های کیفی و تعاملی سیستم می‌پردازد؛ یعنی جایی که پست‌های تحلیلی توسط کارشناسان و مسئولین ارشد، دسته‌بندی، حاشیه‌نویسی و انتساب سازمانی می‌شوند.

۴. معماری کامنت‌های ساختاریافته (Structured Commenting System)
در پلتفرم سحاب، بخش دیدگاه‌ها (Comments) از یک سیستم متنی ساده خارج شده و به یک ابزار حاشیه‌نویسی تحلیلی چندلایه تبدیل شده است. این تفکیک با استفاده از افزونه‌ی ACF در سطح دیدگاه‌ها مدیریت می‌شود.

مکانیک ذخیره‌سازی در دیتابیس
برخلاف متدهای سنتی که نوع کامنت را در جدول اصلی دیدگاه‌ها دستکاری می‌کنند، پلتفرم سحاب منطق دسته‌بندی را به طور کامل در جدول متاداده‌ی کامنت‌ها (wp_commentmeta) نگهداری می‌کند.

کلید فنی متاداده (meta_key): comment_type

کلید نگاشت ACF (سیستمی): _comment_type (حاوی مقدار هش فیلد: field_6a50f060ebcfb)

کاتالوگ مقادیر مجاز لایه تحلیلی کامنت‌ها
هر توسعه‌دهنده یا هوش مصنوعی برای فیلتر کردن، تحلیل یا بسته‌بندی کامنت‌ها در ماژول‌های خروجی، باید داده‌های ستون meta_value را بر اساس این کاتالوگ مپ کند:

مقدار فنی در دیتابیس (meta_value)	برچسب کاربری (فارسی)	کاربرد و منطق معنایی در سیستم
rewrite	بازنویسی خبر	اصلاح، ویرایش یا بازنویسی متن اصلی خبر بر اساس تحلیل جدید.
note	ملاحظه	افزوده شدن نکته، حاشیه یا ملاحظه‌ای خاص بر روی محتوای سند.
theory	نظریه	ارائه تحلیل کلان، فرضیه یا نظریه کارشناسی روی ابعاد رویداد.
فاقد رکورد (Null/Empty)	هیچ‌کدام (متفرقه)	کامنت‌های عمومی که در هیچ‌یک از دسته‌های بالا قرار نمی‌گیرند.
نمونه کوئری بازیابی متاداده کامنت برای موتور سینک
SQL
-- استخراج نوع تحلیل یک کامنت مشخص
SELECT meta_value 
FROM wp_commentmeta 
WHERE comment_id = [COMMENT_ID] AND meta_key = 'comment_type';
۵. فیلد انتساب و ساختار سازمان‌دهی اداری (Organizational Routing)
برای مدیریت جریان کار و مشخص کردن اینکه هر سند تحلیلی یا خبر به کدام بخش یا مسئول ارجاع داده شده است، از فیلد اختصاصی انتساب سازمانی استفاده می‌شود.

نام فنی فیلد (meta_key): reports_to

نوع فیلد در محیط مدیریت: کاربر (User Field)

نوع داده ذخیره شده در wp_postmeta: شناسه عددی یکتای کاربر در وردپرس (User ID / Integer).

منطق و محدودیت‌های فیلد (Validation & Rules)
سطح دسترسی محدود: این فیلد در لایه فرانت‌اِند و بک‌اِند مجهز به فیلتر نقش است. سیستم به صورت ساختاری فقط کاربرانی را برای انتخاب لیست می‌کند که دارای نقش رسمی «ویرایشگر» (Editor) در وردپرس باشند (که در منطق سحاب معادل مسئول اداره تعریف می‌شوند).

بهینه‌سازی ذخیره‌سازی: سیستم به هیچ عنوان آبجکت‌های سنگین کاربر (شامل ایمیل، پسورد، نام و...) را در متادیتای پست ذخیره نمی‌کند. دیتابیس تنها حاوی یک عدد (ID) است.

منطق برنامه‌نویسی برای استخراج اطلاعات مسئول اداره
هرگاه سیستم هوش مصنوعی یا افزونه‌ی جانبی بخواهد بداند سند به کدام مسئول ارجاع شده، ابتدا ID را از فیلد reports_to خوانده و سپس مشخصات فردی را فراخوانی می‌کند:

PHP
// دریافت شناسه مسئول اداره
$manager_id = get_post_meta($post_id, 'reports_to', true);

if (!empty($manager_id)) {
    // فراخوانی پروفایل کاربر از هسته وردپرس بدون نیاز به کوئری مستقیم دیتابیس
    $manager_data = get_userdata($manager_id);
    $manager_display_name = $manager_data->display_name;
    $manager_email = $manager_data->user_email;
}
این پایان پارت دوم مستندات است. در پاسخ بعدی (پارت سوم و نهایی)، پروتکل همگام‌سازی افزونه‌ی sahab-sync، فرمت استاندارد خروجی JSON و سیستم اعتبارسنجی غیرمتمرکز را مستند خواهیم کرد. در صورت تایید، بفرمایید تا پارت سوم را ارسال کنم.


مستندات فنی و مرجع جامع پلتفرم «سحاب» (پارت سوم - نهایی)
این بخش از سند، پروتکل همگام‌سازی، فرمت استاندارد تبادل داده (Data Exchange Format) و مکانیزم‌های اعتبارسنجی افزونه‌ی اختصاصی sahab-sync را تبیین می‌کند تا هر سیستم یا هوش مصنوعی جانبی بتواند پکیج‌های داده را کاملاً منطبق بر منطق سحاب تولید یا مصرف کند.

۶. پروتکل همگام‌سازی غیرمتمرکز (sahab-sync Protocol)
افزونه‌ی sahab-sync وظیفه انتقال و یکپارچه‌سازی داده‌ها میان لایه‌های مختلف یا سرورهای همتا را بر عهده دارد. از آنجا که دیتابیس‌ها به صورت مستقیم به یکدیگر متصل نیستند، معماری سینک بر دو اصل استوار است:

شناسه یکتای جهانی (UUID): به جای تکیه بر IDهای خودکار وردپرس (که در دیتابیس‌های مختلف متفاوت است)، هر پست و کامنت تحلیلی دارای یک متادیتا به نام sahab_uuid است که اصالت و یکتایی آن را در تمام شبکه‌ها تضمین می‌کند.

جلوگیری از تداخل با Content Hashing: هر پکیج داده حاوی یک ساختار هش (مانند MD5 یا SHA-256) از کل محتوا است. سیستم مقصد ابتدا هش را مقایسه کرده و تنها در صورت تغییر در محتوا، عملیات به‌روزرسانی (Update) را انجام می‌دهد.

۷. فرمت استاندارد خروجی و ورودی پکیج داده (JSON Payload Schema)
هر توسعه‌دهنده یا هوش مصنوعی که قصد دارد خروجی سیستم را دریافت کند یا داده‌ای را به پلتفرم سحاب تزریق کند، باید پکیج ساختاریافته‌ی خود را دقیقاً بر اساس مدل JSON زیر پیاده‌سازی کند. این مدل تجمیع‌کننده‌ی تمام فیلدهای تحلیلی، پیوست‌ها و ساختار سازمانی است که در پارت‌های قبلی تعریف شدند:

JSON
{
  "system": "Sahab Platform",
  "version": "1.0.0",
  "sync_timestamp": 1784532824,
  "payload": {
    "post_data": {
      "uuid": "f81d4fae-7dec-11d0-a765-00a0c91e6bf6",
      "title": "گزارش واکاوی تحرکات منطقه‌ای",
      "content": "متن اصلی گزارش تحلیلی و واکاوی رویداد در این بخش قرار می‌گیرد...",
      "status": "publish",
      "core_metadata": {
        "automation_id": 140502,
        "sahab_reg_date_shamsi": "1405/04/29",
        "subject": ["political-clerics", "dogmatism"],
        "event_date": "1405/04/25",
        "classification": "confidential",
        "priority": "urgent",
        "news_type": "technical",
        "evaluation": "probably_valid"
      },
      "organizational_routing": {
        "reports_to_user_id": 5,
        "assigned_role_filter": "editor"
      },
      "attachments": [
        {
          "field_key": "attachment_file_1",
          "attachment_id": 412,
          "file_url": "http://sahab.local/wp-content/uploads/2026/07/report-01.pdf"
        },
        {
          "field_key": "attachment_file_2",
          "attachment_id": 413,
          "file_url": "http://sahab.local/wp-content/uploads/2026/07/appendix.zip"
        },
        {
          "field_key": "attachment_file_3",
          "attachment_id": null,
          "file_url": null
        }
      ],
      "structured_comments": [
        {
          "comment_uuid": "c9a64f32-bc11-49e1-88d4-20387532ac11",
          "author": "کارشناس واکاوی ۱",
          "content": "ملاحظه فنی: متن این خبر نیاز به بازبینی لایه‌های سایبری دارد.",
          "date_gmt": "2026-07-20 05:30:00",
          "analysis_metadata": {
            "comment_type": "note"
          }
        },
        {
          "comment_uuid": "d8b75e43-cd22-5af2-99e5-31498643bd22",
          "author": "تحلیل‌گر ارشد",
          "content": "نظریه نهایی: با توجه به شواهد، این رویداد منجر به فرآیندهای فرامنطقه‌ای خواهد شد.",
          "date_gmt": "2026-07-20 06:15:00",
          "analysis_metadata": {
            "comment_type": "theory"
          }
        }
      ]
    }
  }
}
۸. پروتکل اعتبارسنجی داده‌ها (Data Validation Protocol)
برای حفظ یکپارچگی ساختار سیستم بدون دسترسی به دیتابیس، هر ماژول توسعه‌دهنده باید قواعد سخت‌گیرانه (Strict Rules) زیر را در لایه منطق خود اعمال کند:

اعتبارسنجی فیلدهای انتخابی (Enums): مقادیر متادیتاهایی مثل classification یا priority حتماً باید با کلمات انگلیسی کوچک (Lowercase) ارسال شوند و با ساختار کاتالوگ پارت اول مطابقت داشته باشند. ارسال مقادیر فارسی (مانند "محرمانه") مجاز نیست و پکیج را Reject می‌کند.

سلسله مراتب آرایه پیوست‌ها: فیلدهای پیوست حتی در صورت خالی بودن باید با ساختار کلیدهای متوالی attachment_file_1 تا ۳ بررسی شوند. مقادیر تهی باید به صورت null در پکیج مشخص گردند.

نوع متاداداه کامنت: در زمان درج کامنت از طریق لایه سینک، کلید comment_type حتماً باید هم‌زمان با فیلد سیستمی آندرلاین‌دار آن (_comment_type) آپدیت شود تا ساختار ACF در پیشخوان وردپرس پاشیده نشود.

پایان مستندات مرجع جامع پلتفرم سحاب
این سند اکنون به طور کامل تجمیع و در قالب ۳ پارت مدون نهایی شد. هر انسان یا هوش مصنوعی با در اختیار داشتن این مانیفست فنی، بدون نیاز به دسترسی به دیتابیس محلی شما، می‌تواند برای پلتفرم سحاب افزونه بنویسد، فیلدها را مدیریت کند و خروجی‌های سینک استاندارد تولید نماید. مأموریت ما در این بخش با موفقیت به پایان رسید. اگر بخش جدیدی مد نظر دارید یا مایلید سراغ کدهای فرانت‌آند و پوسته فرزند برویم، بفرمایید در خدمتم.